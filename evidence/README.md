# Runtime evidence

`recovery-report.json` is generated by `scripts/local-dr-drill.sh` and is not hand-edited.
The checked-in report is **local synthetic evidence**, never an AWS runtime PASS. Cloud drill
evidence must be generated by the same controller after AWS health, restore, S3, and transaction
checks execute. See [runtime evidence](../docs/runtime-evidence.md).

